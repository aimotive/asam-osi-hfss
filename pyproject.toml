[project]
name = "osi-hfss"
version = "0.0.1"
requires-python = "== 3.12.*"
dependencies = [
    "numpy == 2.5.*",
    "jupyter == 1.1.*",
    "matplotlib == 3.11.*",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]